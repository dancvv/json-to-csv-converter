[
  {
    "context": "Editor && (extension == json || extension == csv || extension == tsv)",
    "bindings": {
      "cmd-shift-p": "task::Spawn",
      "cmd-alt-shift-p": "command_palette::Toggle"
    }
  }
]
